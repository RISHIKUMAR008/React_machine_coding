const items = [ { id: 1, name: "Item 1" }, { id: 2, name: "Item 2" } ];



return (

  <ul>

    {items.map(item => (

      <li key={item.id}>{item.name}</li> // Using 'id' as the unique key

    ))}

  </ul>

);
